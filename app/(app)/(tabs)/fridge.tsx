import { TabPlaceholder } from './index';

import type { JSX } from 'react';

export default function FridgeTab(): JSX.Element {
  return (
    <TabPlaceholder
      description="Fridge inventory screens land in the next household wave."
      title="Fridge"
    />
  );
}
