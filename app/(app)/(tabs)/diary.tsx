import { TabPlaceholder } from './index';

import type { JSX } from 'react';

export default function DiaryTab(): JSX.Element {
  return (
    <TabPlaceholder
      description="Diary logging and summaries arrive in a later wave."
      title="Diary"
    />
  );
}
